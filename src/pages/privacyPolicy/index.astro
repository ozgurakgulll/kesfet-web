---
import Layout from "Layout.astro";
import PrivacyPolicyPage from "@modules/privacyPolicy"; // React TSX component
import defaultTemplateConfig from "utils/config";
---

<Layout {...defaultTemplateConfig} seo={defaultTemplateConfig.privacyPolicy?.seo}>
  <PrivacyPolicyPage client:load config={defaultTemplateConfig} />
</Layout>
